/* Aggregate camera icon assets so the sketch can include one stable entry point.
 * Keeping this file in the sketch root avoids Arduino build issues with subfolders.
 */
#include "img/cam.c"
#include "img/esc.c"
